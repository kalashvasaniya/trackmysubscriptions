# ===========================================
# TrackMySubscriptions Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# Never commit .env.local to version control!

# ===========================================
# DATABASE (Required)
# ===========================================
# MongoDB connection string
# Get one free at: https://www.mongodb.com/atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/trackmysubscriptions?retryWrites=true&w=majority

# ===========================================
# AUTHENTICATION (Required)
# ===========================================
# NextAuth.js configuration
# NEXTAUTH_URL: Your app's URL (use http://localhost:3000 for development)
# NEXTAUTH_SECRET: Generate with: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-super-secret-key-generate-with-openssl

# Google OAuth credentials
# Get these from: https://console.cloud.google.com/apis/credentials
# 1. Create a new project or select existing
# 2. Go to "Credentials" > "Create Credentials" > "OAuth client ID"
# 3. Select "Web application"
# 4. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ===========================================
# EMAIL NOTIFICATIONS (Optional)
# ===========================================
# Resend for sending email notifications
# Get your API key at: https://resend.com
# Note: Without these, email features (payment reminders, welcome emails) will be disabled
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_EMAIL_FROM=hello@yourdomain.com
RESEND_EMAIL_REPLY_TO=support@yourdomain.com

# ===========================================
# CRON JOBS (Optional)
# ===========================================
# Secret for authenticating cron job requests
# Used to protect the /api/alerts/send endpoint
# Generate with: openssl rand -base64 32
CRON_SECRET=your-cron-secret-for-scheduled-tasks

# ===========================================
# CURRENCY CONVERSION (Optional)
# ===========================================
# API for real-time currency exchange rates
# Options:
# - https://exchangerate-api.com (free tier available)
# - https://openexchangerates.org
# - https://currencyapi.com
# Can be an API key or full URL endpoint
EXCHANGE_RATE_API_KEY=your-exchange-rate-api-key
